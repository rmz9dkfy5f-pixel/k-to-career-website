{
  "permissions": {
    "allow": [
      "Bash(set -e)"
    ],
    "deny": [
      "Bash(git push --force*)",
      "Bash(git filter-branch*)",
      "Bash(git filter-repo*)",
      "Bash(rm -rf /)"
    ]
  }
}
