# Status

> **Pointer only — no content lives in this file.**
> This repository keeps its canonical documentation where `CLAUDE.md`'s Canonical Repo Structure
> section puts it. `docs/project/` exists because Project Starter Kit V3.4 expects these paths;
> duplicating content here would create the competing sources of truth `CLAUDE.md` prohibits.

## Canonical source

Current project state is tracked in the AntBrainOS vault, not in this repository:

```text
03_PROJECTS/Active/K_to_Career_Website/CURRENT_CONTEXT.md
03_PROJECTS/Active/K_to_Career_Website/HANDOFF_TO_CLAUDE.md
```

Release history is in the repository's root `CHANGELOG.md`.

Repository health as of the last check is in `docs/governance/REPO_HEALTH_CHECK.md`.

## Note for agents without vault access

Those vault paths live on a machine-local volume and are unreachable from other machines or from a
fresh clone. This is a known gap, recorded as R-002 in `docs/governance/PROJECT_RISK_REGISTER.md`.
If you cannot read them, say so rather than reconstructing state from guesswork.
